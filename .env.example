# Database (passwords live in ./secrets/, not here — see README)
MYSQL_DATABASE=wordpress
MYSQL_USER=wordpress

# Redis object cache
REDIS_PASSWORD=change_me_redis_password

# Optional: rclone remote for offsite backup copies (e.g. b2:my-bucket/wordpress)
#RCLONE_REMOTE=
